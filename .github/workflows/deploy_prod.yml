# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploying Website Production
on:
  push:
    branches: [main]
jobs:
  Build_Deploy_Web:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x]
    env:
      CI_FIREBASE_MESSAGE: ${{ github.event.head_commit.message }} - ${{ github.event.head_commit.url }}/checks
    steps:
      - uses: actions/checkout@v1
      - run: yarn build
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: yarn install
        run: |
          yarn
      - name: Run tests
        run: |
          CI=true
      - name: Build & Deploy
        run: |
          yarn build
          firebase deploy --only hosting --token ${{ SECRETS.FIREBASE_TOKEN }} --message "$CI_FIREBASE_MESSAGE"
